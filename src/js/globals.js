export const separatorSystem = {
  enabled: true,
  syncWithReaper: true,
  separators: {},
  nextId: 1,
};

/**
 * @type {import('./types').Globals}
 */
export const globals = {
  masterOnLeft: true,
  isConnected: false,
  hiddenTracks: {},
  masterChannel: null,
  currentMenuChannel: null,
  channels: {},
};
